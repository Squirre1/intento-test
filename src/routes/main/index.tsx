import { FunctionalComponent, h } from "preact";

import Translator from "../../components/translator";

const Main: FunctionalComponent = () => (
    <div>
        <p>Use intento translate api</p>
        <Translator />
    </div>
);

export default Main;
